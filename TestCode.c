#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "TestCode.h"

/*
void main()
{
    char letter = 'A';
    char *binary_letter = malloc(sizeof(char)+1);
    strcpy(binary_letter, char_to_binary(letter));
    printf("%s, %d ",binary_letter,sizeof(char));
}
 */